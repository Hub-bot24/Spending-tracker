const VERSION = 'v2';
const PREFIX = '/Spending-tracker';
const ASSETS = [
  `${PREFIX}/`,
  `${PREFIX}/index.html`,
  `${PREFIX}/manifest.webmanifest`,
  // cache Food page too
  `${PREFIX}/food/`,
  `${PREFIX}/food/index.html`
];
